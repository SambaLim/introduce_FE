{
  "name": "nextra",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "next -p 12000",
    "start": "next start",
    "build": "next build && next export",
    "deploy": "next build && next export && aws s3 sync ./out s3://feclass --profile test-peer"
  },
  "author": "Samba Lim",
  "license": "MIT",
  "dependencies": {
    "next": "^11.0.1",
    "nextra": "^0.4.1",
    "nextra-theme-docs": "^1.1.2",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "prettier": "^2.0.5"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true
  }
}
